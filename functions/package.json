{
  "name": "openvan-functions",
  "version": "1.0.0",
  "scripts": { "deploy": "firebase deploy --only functions" },
  "engines": {"node": "20"},
  "dependencies": {
    "firebase-functions": "^6.3.0",
    "node-fetch": "^3.3.2"
  }
}
